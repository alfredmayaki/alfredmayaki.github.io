name = "shy-sea-600a"
main = "src/index.js"
compatibility_date = "2026-01-09"
workers_dev = true

[vars]
GEMINI_MODEL = "gemini-1.5-flash"

# Optional: if you later bind a custom domain/route, add:
# routes = ["https://example.com/chat*"]