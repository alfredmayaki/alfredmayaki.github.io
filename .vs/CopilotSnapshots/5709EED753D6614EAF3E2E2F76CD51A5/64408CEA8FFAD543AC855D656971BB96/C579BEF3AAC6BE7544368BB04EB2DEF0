# Copilot Instructions

## General Guidelines
- First general instruction
- Second general instruction
- Commit changes using GitHub Desktop to ensure version control and collaboration efficiency. User has successfully committed changes to the repository using GitHub Desktop.
- Prefer precise, line-based edits to files, replacing specific lines with specific lines as needed. Apply suggested edits (e.g., wrapping intro paragraphs in a `.container-grid` and adding corresponding CSS) directly to the current file (msc-survey.html) and prefer one-step patching when changes are needed.

## Navigation/Select Menu Behavior
- Make the quick navigation menu (`navigationSelect`) behave identically to the language menu (`languageSelect`): use the same CSS theme and a change handler that navigates via `window.location.href`; remove special cases for '#chatbot' and opening external links in new tabs.

## Chat UI Preferences
- Align bot messages to the left and user messages to the right, with cascading chat bubble offsets for alternating sides.
- Use distinct bubble styles for bot and user messages, ensuring bubbles are rounded and animated. User prefers modern, polished right-side (user) chat bubble styling for the chatbot UI. Revert recent UI change to `index.html` (chat bubble styling) when requested.
- Implement entrance animations for messages.
- Ensure input and upload controls are rounded, centered, and have a maximum width of 360px.
- Prefer native browser styling for `<select>` controls, avoiding custom select styles and styling for `<option>`. Use browser defaults to prevent inverse/native rendering issues. Maintain consistent caret styling, use `appearance: none` on selects, and apply accent-color (brand blue) for additional emphasis. User expects hover to show brand-blue without inverse colors. Reset selects to native browser styling by default, avoiding custom select styling for navigationSelect and languageSelect. User prefers droplist/select controls to be slightly larger (increased font-size, padding, min-height) and horizontally centered on the page; increase select font-size, padding, and min-height; center `.dropdown-group` on the page. On small screens, selects should become full-width.
- Apply the dropdown/select visual theme from index_en.html (the `.profile-links-select` styling) to the droplists in index.html (navigationSelect and languageSelect), making selects dark, rounded, with custom caret SVG, hover and focus styles, and slightly larger max-width (≈400px).
- Prefer chatbot popup to be responsive on mobile: use full-width inset (12px) with max-height ~70vh, touch-friendly controls (larger buttons), input and upload controls full-width, and scrollable messages area. Ensure minimized state still works on mobile.
- Prefer black skin tone emojis in the chat UI.
- When wiring page-level file uploads to the chatbot, always attach uploads to chatbot state synchronously and use robust feature-detection. Initialize `window.uploadedDocs` if missing; guard calls to helper functions like `extractFile`, `pushChatMessage`, `renderUploadList`; prefer calling `window.__setUploadedFile` and `window.__openChatbot`.

## Contact Page Preferences
- User prefers Font Awesome icons for social links on the contact page.
- Replace the Discord invite in the footer with a 'report a bug' link (linking to the contact page) instead of keeping the Discord icon; prefer a single 'report a bug' entry rather than duplicates.

## User Interaction Preferences
- After Google authentication, reveal the embedded survey on the same page (hide login and show survey-section) and enable the Submit button immediately; backend sessionLogin may run in the background but must not redirect the user away to /dashboard.